services:

  # The TestShop application (pull from GHCR)
  app:
    image: ghcr.io/bqnow/testshop:${SHOP_IMAGE_TAG:-latest}
    ports:
      - "3000:3000"
    environment:
      - HOSTNAME=0.0.0.0
    healthcheck:
      test: [ "CMD", "wget", "--spider", "-q", "http://127.0.0.1:3000" ]
      interval: 5s
      timeout: 5s
      retries: 5

  # Maven test runner with Playwright Java
  maven:
    build:
      context: .
      dockerfile: Dockerfile
    depends_on:
      app:
        condition: service_healthy
    environment:
      - BASE_URL=${BASE_URL:-http://app:3000}
      - CI=true
      - SKIP_WEBKIT=true
      - TEST_USER_NAME
      - TEST_USER_PASSWORD
    volumes:
      - ./target:/app/target
